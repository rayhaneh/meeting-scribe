# secrets / local config
.env

# transcripts & recordings (don't commit meeting content)
*.txt
*.wav
*.m4a
*.mp3
*.mp4
transcripts/

# keep the recordings/ folder, ignore everything inside it
recordings/*
!recordings/.gitkeep

# os cruft
.DS_Store
